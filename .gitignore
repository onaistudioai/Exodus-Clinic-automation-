/node_modules
/.next
/out
/build
next-env.d.ts
*.tsbuildinfo

# env — NUNCA commitar (cofre: D:\projetos\.credentials\)
.env
.env.local
.env*.local

.DS_Store

.vercel
.env*

# Arquivos de EXEMPLO devem ser versionados (nao contem segredo, so os nomes
# das variaveis). Sem isto, um deploy novo sobe sem saber o que configurar.
!.env*.example
